{
	# Local dev: use Caddy's internal CA. Trust the root once with:
	#   caddy trust  (requires the stack to be running)
	local_certs
	admin 0.0.0.0:2019 {
		origins localhost:2019
	}
}

local.tickr.keithheacock.com {
	tls internal

	handle /api/* {
		reverse_proxy api:3000
	}

	handle /ws/* {
		reverse_proxy api:3000
	}

	handle {
		reverse_proxy host.docker.internal:5173
	}
}
