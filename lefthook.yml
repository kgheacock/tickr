pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx,js,cjs,mjs}'
      run: fnm exec -- pnpm exec eslint {staged_files}
    format:
      glob: '*.{ts,tsx,js,cjs,mjs,json,md,yml,yaml}'
      run: fnm exec -- pnpm exec prettier --check {staged_files}
